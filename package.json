{
  "name": "eduscore",
  "version": "1.0.0",
  "description": "Plataforma integrada de analytics educacional: da digitalização de folhas de resposta à predição de desempenho usando machine learning",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:local": "nodemon --config nodemon.local.json server.js",
    "migrate": "node backend/migrations/create_schema.js",
    "postinstall": "if command -v python3 >/dev/null 2>&1; then python3 -m pip install --upgrade pip && python3 -m pip install -r backend/scripts/requirements.txt || true; elif command -v python >/dev/null 2>&1; then python -m pip install --upgrade pip && python -m pip install -r backend/scripts/requirements.txt || true; else echo '⚠️  Python não encontrado no PATH. Certifique-se de que Python 3 está instalado.'; fi"
  },
  "keywords": [
    "education",
    "analytics",
    "machine-learning",
    "educational-platform",
    "exam-management",
    "performance-prediction",
    "student-tracking",
    "vestibular",
    "amazonas",
    "sqlite",
    "express",
    "jwt"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@libsql/client": "^0.15.2",
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "form-data": "^4.0.5",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
